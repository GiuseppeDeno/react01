export const Header = ({name})=>{
    return (
 <header className="header">
        <h1>Welcome to Simple Blog of {name}</h1>
        <p>A clean, minimal blog template showcasing articles in a two-column layout with engaging content and responsive design.</p>
      </header>

    );
};
export default Header;